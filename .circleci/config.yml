version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run Rock Paper Scissors Game
          command: python rock_paper_scissors.py rock
      - run:
          name: Check Python Syntax
          command: python -m py_compile rock_paper_scissors.py